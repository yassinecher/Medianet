'use client'
import { MedianetLogo } from './MedianetLogo'

/** Header variant of the logo — same artwork as {@link MedianetLogo}. */
export function MedianetLogoMain(props: React.ComponentProps<typeof MedianetLogo>) {
  return <MedianetLogo {...props} />
}
